# Xum ayron bot (python-telegram-bot)

## Ishga tushirish
1) Python 3.10+ o'rnating.
2) `pip install -r requirements.txt`
3) `BOT_TOKEN` env qo'ying:

```powershell
$env:BOT_TOKEN="YOUR_BOT_TOKEN"
```

4) Ishga tushirish:

```powershell
python main.py
```

## Admin
- `/admin` → parol: `123456` (o'zgartirish uchun `ADMIN_PASSWORD` env qo'ying)

## Userlar ro'yhati
- `USERLAR RO'YHATI TEXT` tugmasi userlar ro'yhatini matn ko'rinishida chiqaradi.
